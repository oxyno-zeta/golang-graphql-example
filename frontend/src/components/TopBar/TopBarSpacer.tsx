import React from 'react';

function TopBarSpacer() {
  return (
    <div
      style={{
        height: '43px', // This size is the app bar dense mode size. This isn't present in the theme so...
      }}
    />
  );
}

export default TopBarSpacer;
